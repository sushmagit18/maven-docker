pipeline {
    agent any
    triggers{
        pollSCM '* * * * *'
    }
    stages{
        stage ("Maven-Build"){
            steps{
                sh '''

                '''
            }
        }
    }
}